# Apollo Risk Note Template

Risk:
- `<one-line summary>`

Why it is risky:
- `<routing order | threshold tuning | schema drift | UI fragility | concurrency | external service dependency>`

What changed:
- `<short description>`

Mitigation:
- `<tests added, guards preserved, fallback kept>`

Watch for:
- `<specific regression signals or commands likely to break>`
